Inside the addons folder create a folder(I called mine "CustomPizza")
Create a "lua" file inside the "CustomPizza" folder
Create an "entities" folder inside "lua" folder
Create 5 folder inside the "entities" folder(I named them "table", "dough", "tomato", "cheese", "pizza"
Inside each one of those folders create the "cl_init.lua", "init.lua", "shared.lua"